# Guideline: Database Tables

This document outlines how to interact with the database to access stored data and perform queries.

## Database

This project does not currently have a remote database to interact with. All data is stored in the browser's `localStorage`.
